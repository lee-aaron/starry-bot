{
	"name": "starry-bot",
	"version": "0.1.0",
	"description": "",
	"private": true,
	"scripts": {
		"dev": "cd core/ui && cargo run --features local",
		"dev:watch": "cd core/ui && cargo watch -x 'run --features local'",
		"build": "cd core/ui && cargo build --release",
		"clean": "cargo clean"
	}
}
